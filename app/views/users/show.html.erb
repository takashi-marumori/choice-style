<div class="set-style">
<%= "#{@user.nickname}さんのマイページ" %>
</div><br />
  <div class="style-set-main">
  <h2 class="style-set-title">これまでのスタイル一覧</h2>
    <div class="item-box">
      <% @posts.each do |posts| %>
        <%= link_to post_path(posts) do %>
          <% image_tag posts.image, class:"image-small"%>
        <% end %>
      <% end %>
    </div>
    <%= link_to 'もどる', root_path %>
  </div>